Generate a valid Nuclei template in YAML format to detect the vulnerability described below. The template must:

1. Use the exact CVE ID provided in the 'id' field
2. Include a meaningful name and description based on the CVE description
3. Set appropriate severity (low, medium, high, critical)
4. Include relevant tags
5. Create a realistic HTTP request with appropriate payload
6. Add matchers that would detect the vulnerability
7. Use proper YAML indentation (2 spaces)
8. Return ONLY the YAML code, no markdown formatting, no comments, and no explanations

CVE ID: {cve_id}
Description: {description}

Template structure:
id: {cve_id}
info:
  name: [Descriptive name based on vulnerability]
  author: nuclei-api
  severity: [low/medium/high/critical]
  description: [Description based on CVE details]
  reference:
    - https://nvd.nist.gov/vuln/detail/{cve_id}
  tags: [relevant,tags,here]
requests:
  - method: [GET/POST]
    path:
      - "{{BaseURL}}/[vulnerable-endpoint]"
    [headers: (if needed)]
    [body: (if POST request)]
    matchers:
      - type: word
        words:
          - "[indicator1]"
          - "[indicator2]"
        part: body
        condition: or

Generate the template now: